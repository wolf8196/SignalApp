<mah:MetroWindow
    x:Class="SignalApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SignalApp"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SignalApp"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="8*" />
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label Margin="8" Content="Host:" />
                <TextBox
                    Width="120"
                    Height="1"
                    Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Text="{Binding Host}" />
                <Label Margin="8" Content="Port:" />
                <TextBox
                    Width="120"
                    Height="1"
                    Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Text="{Binding Port}" />
                <Button
                    Width="120"
                    Height="25"
                    Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Command="{Binding ConnectCommand}"
                    Content="Connect" />
                <Button
                    Width="120"
                    Height="25"
                    Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Command="{Binding DisconnectCommand}"
                    Content="Disconnect" />
                <Label Margin="8" Content="State:" />
                <Label Margin="8" Content="{Binding State}" />
            </StackPanel>


            <TextBlock
                Height="25"
                Margin="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Foreground="{DynamicResource MahApps.Brushes.Control.Validation}"
                Text="{Binding ErrorMessage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <DataGrid
            Grid.Row="2"
            Grid.Column="0"
            AutoGenerateColumns="False"
            ColumnWidth="*"
            IsReadOnly="True"
            ItemsSource="{Binding Records}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Timestamp}"
                    Header="Created On" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FrequencyMegaHz}"
                    Header="Frequency, MHz" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding BandwidthKiloHz}"
                    Header="Bandwidth, kHz" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding SignalNoiseRatio}"
                    Header="SNR, dB" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Count}"
                    Header="Count" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding IsLive}"
                    Header="IsLive" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</mah:MetroWindow>